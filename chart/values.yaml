image:
  repository: ghcr.io/0bu/tibber-pulse-bot
  tag: 1.0.35@sha256:343010596d90c8d8b088f986f84fae0b578d19a4e401fc6976f84446d0ee2969
  pullPolicy: IfNotPresent

# Tibber Pulse Bridge connection
pulse:
  host: ""              # required, e.g. 192.168.107.118
  node: 1

  # --- Password supply (pick exactly one) ---------------------------------
  #
  # 1) inline plain password (dev only — DO NOT commit to git)
  password: ""
  #
  # 2) sealed-secret ciphertext checked into git (recommended for GitOps);
  #    requires the bitnami sealed-secrets controller running in-cluster.
  #    Encrypt with:
  #      echo -n 'XXXX-XXXX' | kubeseal --raw \
  #        --name <release-name> --namespace <ns> \
  #        --controller-namespace kube-system
  #    paste the output here:
  sealedSecret:
    encryptedPassword: ""
    # Optional: override the SealedSecret scope. Default scope is
    # "strict" (name + namespace must match). Use "namespace-wide" or
    # "cluster-wide" if your kubeseal command used --scope.
    scope: ""
  #
  # 3) reference a Secret you create out-of-band (external-secrets,
  #    sops-flux, plain `kubectl create secret`, etc.). Must contain
  #    a key `TIBBER_PULSE_PASSWORD`.
  existingSecret: ""
  # ------------------------------------------------------------------------

# MQTT broker target
mqtt:
  host: ""              # required, e.g. mosquitto.default.svc.cluster.local
  port: 1883
  topic: "tibber/pulse"
  clientID: "tibber-pulse-bot"

# Home Assistant MQTT-Discovery — readings and diagnostics become entities
# under one device named after the meter serial.
homeAssistant:
  discovery: false
  discoveryPrefix: "homeassistant"

# Acquisition mode: "push" (WebSocket, recommended) or "poll"
mode: push
pollInterval: 10s
wsIdleTimeout: 60s
reconnectDelay: 100ms

# Bridge diagnostics: publish one reduced JSON document under
# <topic>/diagnostics on this cadence. Set 0 to disable.
metricsInterval: 60s

# Suppress per-update stdout line when publishing to MQTT
quiet: false

# Verbose: log every WebSocket reconnect (default: only real errors)
verbose: false

# Standard chart knobs
fullnameOverride: ""
replicaCount: 1
resources:
  requests:
    cpu: 10m
    memory: 16Mi
  limits:
    memory: 64Mi

nodeSelector: {}
tolerations: []
affinity: {}

podAnnotations: {}
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 65532
  runAsGroup: 65532
securityContext:
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false
  capabilities:
    drop: [ALL]
